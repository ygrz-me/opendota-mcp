# OpenDota API Configuration
# All settings are optional - the server works without any configuration

# Optional: API key for higher rate limits (get from: https://www.opendota.com/api-keys)
# OPENDOTA_API_KEY=your_api_key_here

# Optional: Set custom API base URL (default: https://api.opendota.com/api)
# OPENDOTA_BASE_URL=https://api.opendota.com/api

# Optional: Set request timeout in milliseconds (default: 30000)
# OPENDOTA_TIMEOUT=30000

# Optional: Set custom User-Agent header
# USER_AGENT=OpenDota-MCP-Server/1.0.0

# Optional: Enable debug logging (default: false)
# DEBUG=false

# Optional: Set log level (default: info)
# LOG_LEVEL=info